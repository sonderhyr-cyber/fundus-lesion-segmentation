# PyTorch must be installed separately with the correct CUDA version.
# On AutoDL, PyTorch + CUDA is pre-installed — do NOT reinstall via pip.
# Locally (CPU / MPS):
#   pip install torch
#
# On a fresh CUDA server without a pre-built image:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118

numpy>=1.24.0,<2.0.0
opencv-python>=4.7.0
matplotlib>=3.7.0
pyyaml>=6.0
timm>=0.9.16,<2.0

# Only used by src/model/test_aupr.py to cross-check our streaming histogram AP
# against a reference implementation. evaluate.py itself does NOT import sklearn
# (the reference cannot handle a full-resolution split in memory).
scikit-learn>=1.3.0
